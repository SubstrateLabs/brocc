import { CodeBlock, InlineCode } from "@/components/mdx/CodeBlock";

To prevent losing your tabs when Brocc relaunches Chrome, check your startup settings:

1. Copy this URL and paste it into your Chrome address bar: <InlineCode code={`chrome://settings/?search=startup`} />

2. Select **Continue where you left off**.
